<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dongogong.dao.mybatis.mapper.UserInfoMapper">
   <cache />

   <insert id ="insertUser">
      INSERT INTO USERINFO (USER_ID, NAME, NICK_NAME, PHONE, PASSWORD, CREATE_DATE_TIME, MODIFIED_DATE_TIME)
      VALUES(#{user_id}, #{name}, #{nick_name}, #{phone}, #{password}, #{create_date_time}, #{modified_date_time})
   </insert>
   
   <update id="updateUser">
      UPDATE USERINFO SET
         NAME = #{name},
         NICK_NAME = #{nick_name},
         PHONE = #{phone},
         PASSWORD = #{password}
      WHERE USER_ID = #{user_id}
   </update>
   
   <delete id="deleteUser">
      DELETE FROM USERINFO
      WHERE USER_ID = #{user_id}
   </delete>
   
   <select id="findUser" resultType="UserInfo">
      SELECT NAME, NICK_NAME, PHONE, PASSWORD
      FROM USERINFO
      WHERE USER_ID = #{user_id}
   </select>
   
   <!--  
   <select id="findUserList" resultType="List<User>">
      SELECT
         USER_ID, NAME, NICK_NAME, PHONE, PASSWORD, INDIVIDUAL_COPORATION_STATUS
      FROM USERINFO
      ORDER BY USER_ID
   </select>
   -->
</mapper>